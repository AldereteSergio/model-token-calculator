# Code Review — Commit `ee21160`

**Fecha:** 2026-07-09  
**Rama:** `main` (1 commit ahead of `origin/main`)  
**Alcance:** `git show HEAD` (último commit)  
**Commit:** `ee21160` — *feat: enhance model form and notification handling in App component*  
**Autor:** Sergio Alderete  
**Archivos:** `.gitignore`, `documents/reviews/2026-07-09-working-tree-app-tsx.md`, `src/App.tsx` (+769 / −234 líneas)  
**Build:** `tsc && vite build` — OK

---

## Review Summary

Commit grande y bien encaminado: incorpora la mayoría de los fixes del review previo del working tree (`last30CoversAll`, validación de precios, cleanup de notificaciones, empates en comparativa 3-way, Escape en modal, `formatDateRangeLabel` mejorado). El build pasa. **Ship con fixes menores** — no hay bloqueantes de seguridad ni correctness en producción, pero conviene revisar el artefacto de review incluido en el mismo commit y la mantenibilidad de `App.tsx`.

---

## Critical (must fix before merge)

*(Sin hallazgos en esta categoría.)*

---

## Suggestions (should fix)

### 1. El review del working tree quedó obsoleto en el mismo commit

**Archivo:** `documents/reviews/2026-07-09-working-tree-app-tsx.md`

Ese documento describe issues que **este mismo commit ya corrige** en `src/App.tsx` (p. ej. `last30CoversAll`, `parsePriceInput` → `null`, timers de notificación, empates 3-way). Quien lea el review commiteado va a creer que el código sigue roto.

**Por qué importa:** documentación contradictoria con el código que la acompaña.

**Fix sugerido:** en un commit de seguimiento, actualizar o reemplazar ese archivo con este review post-commit, o no versionar reviews WIP junto al fix.

---

### 2. `.gitignore` con entradas redundantes

**Archivo:** `.gitignore` (líneas 120–122)

```gitignore
.cursor/
.cursor/rules
.cursor/skills
```

`.cursor/` ya ignora todo el directorio; las dos líneas siguientes no agregan cobertura.

**Fix sugerido:** dejar solo `.cursor/` (o, si en el futuro querés versionar skills, usar excepciones explícitas `!.cursor/skills/`).

---

### 3. Comparativa 3-way: categorías distintas siguen siendo aproximadas

**Archivo:** `src/App.tsx` (aprox. líneas 404–445)

APIs se proyectan a `/mes` desde el ritmo del CSV; Cursor es precio fijo. El disclaimer lo aclara, pero el badge **"Gana"** puede premiar un plan de suscripción contra proyecciones que no reflejan límites de uso reales de Cursor.

**Sugerencia:** considerar etiquetar el ganador como *"Más barato (estimado)"* o separar visualmente API vs suscripción para no sobreinterpretar el resultado.

---

### 4. `App.tsx` sigue siendo un monolito (~1.360 líneas)

**Archivo:** `src/App.tsx`

Helpers (`sanitizePriceInput`, stats, `threeWayCompare`, formateo de fechas) y UI viven en un solo archivo. Funciona hoy, pero cada feature nueva aumenta el costo de review y test.

**Sugerencia:** extraer en un PR posterior `utils/pricing.ts`, `utils/dates.ts` y/o componentes (`ModelModal`, `ThreeWayCompare`) sin cambiar comportamiento.

---

## Nitpicks (optional)

1. **Imports inconsistentes de React** — se importan `useState`, `useMemo`, `useRef` pero los effects usan `React.useEffect`. Unificar con `import { useState, useMemo, useRef, useEffect } from 'react'`.

2. **Empate sin etiqueta explícita** — `winnerKeys` puede tener varios elementos y todos muestran "Gana"; un subtítulo *"Empate"* cuando `winnerKeys.length > 1` ayudaría a leer la UI.

3. **Modelo A y B pueden ser el mismo** — los selects no impiden elegir el mismo modelo en ambos slots; comparativa redundante pero inofensiva.

---

## What looks good

- **Respuesta directa al review anterior:** `last30CoversAll = spanDays <= 30`, `parsePriceInput` devuelve `null` con validación en submit, `notificationTimeoutRef` con cleanup en cadena y en unmount, y empates con `winnerKeys` + tolerancia `0.0001`.
- **Correcciones de fechas y CRUD:** parsing local con `T00:00:00`, ventana de 30 días inclusiva, fix de semanas sin mutar `dateObj`, modal dedicado, delete con `remaining` — todo sólido y alineado con el dominio del proyecto.

---

## Delta vs review working tree (`2026-07-09-working-tree-app-tsx.md`)

| Hallazgo previo | Estado en `ee21160` |
|-----------------|---------------------|
| `last30CoversAll` incorrecto | ✅ Corregido |
| `parsePriceInput` silencioso | ✅ Corregido (`null` + validación) |
| Timers de notificación | ✅ Corregido |
| Empates 3-way | ✅ Corregido (`winnerKeys`) |
| `.gitignore` `.cursor` | ✅ Aplicado (con redundancia menor) |
| `formatDateRangeLabel` redundante | ✅ Mejorado |
| Modal sin Escape | ✅ Corregido |
| Monolito `App.tsx` | ⏳ Pendiente (sugerencia) |

---

## Próximos pasos sugeridos

1. Actualizar o retirar el review WIP obsoleto del repo.
2. Simplificar `.gitignore` de `.cursor`.
3. (Opcional) Modularizar `App.tsx` en un commit separado.
